{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run fly",
      "type": "cppdbg",
      "request": "launch",
      "program": "${workspaceFolder}/build/fly",
      "args": [],
      "cwd": "${workspaceFolder}",
      "MIMode": "gdb",
      "preLaunchTask": "cmake build"
    }
  ]
}
